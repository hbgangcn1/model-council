# MiniShop API 服务

一个演示用的电商 API 服务。

## 版本

v2.1.0

## 快速开始

```bash
pip install -r requirements.txt
python app.py
```

默认端口 **8080**（可在 config.yaml 中覆盖）。

## 依赖

- requests >= 2.28
- flask >= 3.0

## 配置

所有运行参数在 `config.yaml` 中配置。实际生效值以 config.yaml 为准（README 中的端口仅为默认值）。
